export type Skill = {
  name: string;
  icon: string; // Clase de icono (por ejemplo, para FontAwesome)
};

export type Project = {
  id: number;
  title: string;
  description: string;
  image: string;
  url: string;
};
